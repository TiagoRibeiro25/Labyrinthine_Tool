import fallbacks from "./fallbacks.config";
import fastify from "./fastify.config";
import mongoose from "./mongoose.config";
import sources from "./sources.config";

export default { fastify, fallbacks, sources, mongoose };
